# Spezifikation — Historie (Provenance)

Ausgelagerte Änderungs-Historie von [`spezifikation.md`](spezifikation.md)
(slice-018a / `MR-011`). Diese Datei ist **derivative Provenance** und liegt
**außerhalb der `matrix`-Spec-Straten** — so bleiben die ADR-Aufwärts-Verweise
(Regelwerk Regel 5: Provenance am Dokument-Rand) erlaubt **verlinkt**, ohne die
Referenz-Richtungs-Regel der Spec-Straten zu berühren. **Keine eigene
Anforderung**, nicht normativ zitierbar.

| Datum | Änderung | ADR |
|---|---|---|
| 2026-06-08 | Initiale Outline aus Lastenheft-Wertebereichen; Fehler-Codes und OTel-Span-Skelett | Greenfield-Bootstrap |
| 2026-06-11 | §1 `LH-FA-ROM-001`.a präzisiert: Innenkanten-Basis + Ring-Modell, Auslösung bei Modell-Mutation, Endpunkt-Knoten-Einschränkung (welle-1), Erkennung total (kein `E-GEO-002`); §7-Punkt Polygon-Basis geschlossen | [ADR-0007](../docs/plan/adr/0007-raumerkennung-geometrie-basis.md) |
| 2026-06-11 | §1 Kollaps-Kriterium präzisiert: Kantenrichtungs-Erhalt statt reiner Flächen-Prüfung (Doppel-Inversion erzeugt Phantom-Polygon positiver Fläche) | [ADR-0007](../docs/plan/adr/0007-raumerkennung-geometrie-basis.md) |
| 2026-06-11 | §1 `LH-FA-D3-002`.a ergänzt: Benachrichtigungs-Vertrag (Observer-Port, Push-Notify/Pull-State, Reihenfolge nach Re-Detektion, Beobachter-Pflichten) + welle-1-Operationalisierung „sichtbar" | [ADR-0008](../docs/plan/adr/0008-aenderungs-benachrichtigung.md) |
| 2026-06-11 | §1 ROM-001.a präzisiert (Welle-1-Code-Review M1/M2): minimale Zyklen via Flächen-Traversierung — geteilte Knoten (Grad ≥ 3) abgedeckt, Stichkanten ignoriert; Näherung für kollineare Nachbarkanten ungleicher Stärke dokumentiert | [ADR-0007](../docs/plan/adr/0007-raumerkennung-geometrie-basis.md) |
| 2026-06-12 | §1 D3-002.a ergänzt: welle-1v-Operationalisierung „sichtbar" (Qt-Widgets-Fenster, Tessellation über `ViewModelPort`, Szenen-Surrogat, `ACC-002`-Beleg als manueller Abnahme-Schritt) | [ADR-0009](../docs/plan/adr/0009-gui-framework-qt6.md) |
| 2026-06-12 | §1 `LH-FA-WAL-006`.a neu (Eckenschluss-Footprint-Regel, Footprint-Hoheit im Kern, Begrenzung/Rückfälle, EVL-Hinweis Shoelace) + D3-002.a-Mehr-Element-Update (`WallGeometryChanged`, Reihenfolge, Transaktions-Satz) + §3 `WALL_MITER_LIMIT`; zwei WAL-006-Verweise auf Vollumfang präzisiert | slice-012 (Lastenheft 0.1.2) |
| 2026-06-13 | §1 `LH-FA-DOR-004`.a/WIN-005.a neu (Wandöffnung als Schnitt-Prismen im Kern, boolesche Subtraktion über `GeometryKernelPort`, Klemmung/Ablehnung, Totalität/Transaktion, `WallGeometryChanged` der Wirtswand, Raumerkennung/Footprint unberührt) + §3 Tür-/Fenster-/Brüstungs-Wertebereiche | [ADR-0011](../docs/plan/adr/0011-bauteil-hosting-wandoeffnung.md) (slice-013a) |
| 2026-06-13 | §3 Default-Maße bei Tür-/Fenster-Anlage (`DEFAULT_DOOR_*`/`DEFAULT_WINDOW_*`) — Implementierung der Anlage (Muster `DEFAULT_WALL_THICKNESS_MM`) | slice-013b |
| 2026-06-13 | §3 `OPENING_CUT_OVERSHOOT_MM` — Cutter-Überstand für koplanar-freien Boolean (Code-Review-Befund H1: §1-Überstand „je Seite" war nur in Z realisiert, jetzt auch lateral) | slice-013b Code-Review |
| 2026-06-13 | §1 `LH-FA-ROF-001.a` neu (Dach-Geometrie Teilumfang Rechteck-Grundriss: Traufrechteck, Pult/Sattel/Walm-Konstruktion + Höhenformeln, Walm-Einrückbetrag, Firsthöhe abgeleitet, Totalität) + §3 Neigungs-/Überstands-Bereiche + Defaults (= `roofs`-Schema) | slice-014a |
| 2026-06-13 | §1 `LH-FA-SLB-001.a` neu (Platten-Geometrie Decken+Fundament: Polygon × Dicke an `base_z` je `slab_type`, Ausschnitte als Boolean/`CutPrism`, Totalität; Port-base_z-Frage an 015b) + §3 Decken-/Fundament-Dicke-Bereiche + Defaults | slice-015a |
| 2026-06-13 | §1 `LH-FA-SLB-001.a` Port-base_z-Frage geschlossen: kein Port-Wechsel — Mesh-Translation um `base_z` nach dem Boolean, Cutouts relativ `[0,Dicke]`; `SlabChanged`-`op` (storey-bezogen, kein `RoomsChanged`) | slice-015b |
| 2026-06-13 | §1 `LH-FA-SLB-001.a` „auf den Platten-Umriss begrenzt" präzisiert: rand-/außenliegende, degenerierte und nicht-endliche Ausschnitte werden an der API **abgelehnt** (Containment-Vorbedingung) — innenliegende Aussparungen sind damit koplanar-frei, **kein** lateraler Überstand nötig (anders als die Wand durchspannende Öffnung, §1 DOR-004.a) | slice-015b Code-Review (H1) |
| 2026-06-14 | §1 `LH-FA-STR-001.a` neu (Treppen-Geometrie Teilumfang gerade einläufige Treppe: Stufen-Quader-Polyeder im Kern wie `roof_geometry`, `rise = Geschosshöhe/step_count` abgeleitet, feste +x-Aufstiegsrichtung, Geländer als generierte Geometrie ohne Schema-Spalte, `StairChanged`-`op` an `from_storey` gebunden + `stairMeshes` projektweit) + §3 Stair-Wertebereiche (Breite/Stufenanzahl/Auftritt geklemmt, Steigung informativ) + Defaults | slice-016a |
| 2026-06-14 | §1 `LH-FA-EVL-001.a` neu (Auswertungs-Architektur [ADR-0012](../docs/plan/adr/0012-evaluations-architektur.md): `EvaluatePort` read-only/pull; Fläche = Shoelace-Raum-Netto, **Netto-Volumen analytisch im Kern** = Footprint·Höhe − geklemmtes Öffnungsvolumen, **kein** Roh-Prisma/OCC-`GProp`; benannte Miter-Eck-Näherung; Listen-Aggregation über material-tragende `walls`/`roofs`/`slabs`, `windows.frame_material`-Freitext ausgenommen; Material-Auflösungsregel eigenes vs. `wall_type`) + §2.1 `model::Material` + FK-Autorität + §3 `LIVING_AREA_FACTOR` | slice-017a |
| 2026-06-16 | §1 `LH-FA-EVL-001.a` Volumen-Block: „Dach" aus der „aus ihrem analytischen Solid"-Aufzählung **entfernt**; **Dach-Volumen welle-3 zurückgestellt** (dicke-loses Dachmodell → kein Bauteil-Solid; umbauter Raum ≠ Material-Volumen) als benannte Lücke + Re-Eval; EVL-002 welle-3 = **Wand/Decke/Treppe** (`VolumeReport`-Subtotale), Treppen-Geländer ausgenommen, Treppen-Stufenkörper-Formel; **kein** `Solid.volume_mm3`-Lesen (rein analytisch im Kern) | slice-017c |
| 2026-06-16 | §1 Material-Auflösungsregel um den welle-3-Teilumfang erweitert: Material-Verwaltung/-Zuweisung (MAT-001/002/003) projekt-eigen über den Bauteil-Edit-Port (Name-Pflicht leer/whitespace→abgelehnt; `on_delete: restrict` — zugewiesenes Material nicht löschbar; Material-Mutationen op-frei, Pull-Konsum); Auflösungs-**Override** geliefert, **`wall_type`-Template-Fallback zurückgestellt** (kein material-tragender Wandtyp im Domänenmodell, Re-Eval) | slice-017d |
| 2026-06-16 | §1 Material-Block: **Persistenz** ergänzt — `materials`-Bibliothek + `material_id`-Zuweisung (Wand/Dach/Decke) round-trippen über SQLite (atomare Speicher-Transaktion); optionale Kennwerte **NULL-sicher** (kein Wert ⇒ `null`, nicht `0`); `materials.density` ohne Domänenfeld → **nicht** round-getrippt (`NULL` via Spalten-Auslassung) | slice-017e |
| 2026-06-16 | §1 Listen-Block (EVL-004/005/006) präzisiert: Materialliste-**Menge = Netto-Volumen (m³)** über Wand+Decke (EVL-002-Reuse, welle-3-Einheit, nicht mehr „Fläche/Volumen"); **`roofs` aus der Aggregations-Menge in die benannte Lücke verschoben** (Dach material-tragend, aber Volumen zurückgestellt); Tür-/Fensterlisten zählen `openings` mit Maßen (Anzahl = Listengröße) | slice-017f |
| 2026-06-16 | §1 Listen-Block: **Kosten-Auswertung (MAT-006)** ergänzt — je Material `Menge × cost_per_m3` + Projekt-Summe; Material ohne `cost_per_m3` → **keine** Kosten (NULL, nicht 0); **`cost_per_m2`** (Flächen-Kosten) welle-3 nicht genutzt (benannte Lücke, EVL-004 führt Volumen) | slice-017g |
| 2026-06-16 | §1 Material-Auflösungsregel: Inline-Marker „welle-3 zurückgestellt: Override-only" am `wall_type`-Fallback (welle-3-Verifikation LOW-1 — Konsistenz mit „Auflösungs-Teilumfang", kein Über-Versprechen im Präsens-Satz) | welle-3-Closure |
| 2026-06-16 | §1 `LH-FA-IO-001.a` neu (IFC-Import/-Export Subset-Mapping, [ADR-0013](../docs/plan/adr/0013-ifc-bibliothek.md)): IFC-SPF-Encoding (ISO 10303-21), Entitäts-Subset Projekt→Gelände→Gebäude→Geschoss (`IfcRelAggregates`) + gerade Wände, Export IFC4 `IfcWall`+`IfcMaterialLayerSetUsage` (**nicht** deprecated `IfcWallStandardCase`), Import IFC4+IFC2x3, atomarer Import (`E-IO-003`, kein Teil-Import; Export-Schreibrecht `E-IO-001`), Subset-Lücke/Totalität; **§6**-Vertragstabelle IFC-Zeile auf entschiedenen Stand, **§7** IFC-Offene-Punkte-Zeile gestrichen (`ADR-0013`-Folgepflicht) | slice-019a |
| 2026-06-17 | §1 `LH-FA-IO-001.a` kleiner Import-Zusatz (MED-2/3): **Geschoss-Höhe** = Differenz zur nächsthöheren `IfcBuildingStorey.Elevation` (aufsteigend sortiert, Tiebreak Quell-Reihenfolge), Elevation **transient** gelesen/nicht gespeichert (höhen-basiertes Modell, `Storey` ohne `elevation_mm`), oberstes Geschoss → `kDefaultStoreyHeightMm`; Roundtrip-Treue = Anzahl, nicht oberste Höhe (benannte Subset-Grenze). Spec, **nicht** Lastenheft ([MR-008](../harness/conventions.md#mr-008--lastenheft-schärfung-bleibt-lösungsfrei)-zulässig im Impl-Slice) | slice-019b ([ADR-0013](../docs/plan/adr/0013-ifc-bibliothek.md)) |
| 2026-06-17 | §1 `LH-FA-IO-001.a` Export-Spiegel (slice-019c): je `IfcBuildingStorey` wird die **Elevation aus den kumulierten Geschoss-Höhen** geschrieben (unterstes Geschoss 0); **§4** `E-IO-001`-Bedingung um „IFC-Export" erweitert (nicht beschreibbarer Export-Zielpfad → `E-IO-001`/`event=io_no_permission`, atomar, kein Teil-Export) | slice-019c ([ADR-0013](../docs/plan/adr/0013-ifc-bibliothek.md)) |
| 2026-06-17 | §1 `LH-FA-IO-005.a` neu (STEP-/STL-Export-Mapping, [ADR-0014](../docs/plan/adr/0014-step-stl-export-backend.md)): OCC-DataExchange nativ (STEP B-Rep AP214/242 / STL Netz binär), **geometrie-residenter** `ModelExporterPort` (OCC nur in `adapters/geometry/`, Regel C; je Format verdrahtet), atomar (Temp+Rename → `E-IO-001`, kein Teil-Export; OCC-Fehler → neutraler Wurf), Bauteil-Subset 3D-fähige Bauteile/Totalität; **§6**-Vertragstabelle neue STEP/STL-Zeile, **§7** STEP/STL aus der Offene-Backends-Sammelklausel entfernt (DXF/PDF/PNG bleiben offen) — `ADR-0014`-Folgepflicht | slice-020a |
| 2026-06-17 | §1 `LH-FA-IO-005.a` präzisiert (Impl-Befund slice-020b): **STEP** schreibt B-Rep nur für die **OCC-Solid-Bauteile** (Wände + Decken/Fundament, AP214); **Dächer/Treppen** sind analytische Netze ohne OCC-Solid → STEP-**Lücke** (STL-verlustfrei, B-Rep-Vernähung = Folge-Inkrement). **STL** deckt alle 3D-Bauteile. Geschärft, weil das committete §1 B-Rep „wie der Geometrie-Adapter sie baut" für alle Bauteile las — zutreffend nur für Wände/Decken (MED-1 des Plan-Reviews) | slice-020b ([ADR-0014](../docs/plan/adr/0014-step-stl-export-backend.md)) |
| 2026-06-17 | §1 `LH-FA-IO-003.a` neu (DXF-Import/-Export-Mapping, [ADR-0015](../docs/plan/adr/0015-dxf-backend.md)): **2D-Grundriss**, selbst getragener ASCII-DXF-Subset-Codec **io-resident** (`adapters/io/`, kein OCC; symmetrischer Reader+Writer); gerade Wand-Achsen als `LINE`/`LWPOLYLINE` je Geschoss-`LAYER`; **Import-Defaults** (DXF trägt keine Höhe/Dicke → `kDefault*`, Muster IFC-Geschoss-Höhe); atomarer Import (vollständiges In-Memory-Modell zuerst, `E-IO-003` generisch, kein Teil-Import) / atomarer Export (`E-IO-001`); Subset-Lücke (Räume/Bemaßung/Schraffur/Blöcke/Text/Bögen/3D)/Totalität. **§6** neue DXF-Vertragszeile, **§7** DXF aus der Offene-Backends-Klausel entfernt (PDF/PNG bleiben offen), **§4** `E-IO-001`-Bedingung um STEP/STL/DXF-Export erweitert — `ADR-0015`-Folgepflicht | slice-021a |
| 2026-06-18 | §1 `LH-FA-ROF-001.a` um die **Dachdicke** erweitert (`LH-FA-ROF-006`): geschlossener Schräg-Slab der Dicke `d` (Oberseite + um `d` vertikal versetzte parallele Unterseite + geschlossene Trauf-/Giebel-Ränder; wasserdicht/außen-orientiert, alle 3 Typen); **vertikaler** Offset; Dach-Volumen **analytisch im Kern** (`volume_geometry`, ohne `Solid.volume_mm3`); §3 `ROOF_THICKNESS_*` (50/500/200 mm). Macht das Dach zum Volumenkörper → EVL-Dach-Volumen + STEP-B-Rep auflösbar (Folge-Slices) | [ADR-0011](../docs/plan/adr/0011-bauteil-hosting-wandoeffnung.md), [ADR-0012](../docs/plan/adr/0012-evaluations-architektur.md) |
| 2026-06-19 | §1 `LH-FA-IO-005.a` geschärft (slice-024a): **STEP** schreibt jetzt auch **Dächer** als B-Rep — das seit `LH-FA-ROF-006`/slice-023b wasserdichte Dach-Netz wird via OCC `BRepBuilderAPI_Sewing` zu **einem** Solid vernäht (geometrie-resident, `TKShHealing`, keine neue Dependency); **fail-closed** (nicht geschlossen vernähbar → übersprungen, kein Schrott-Solid). **Treppen** bleiben die benannte STEP-Lücke (analytische, nicht-manifolde Box-Union) bis slice-024b | slice-024a ([ADR-0014](../docs/plan/adr/0014-step-stl-export-backend.md)) |
| 2026-06-19 | §1 `LH-FA-IO-005.a` geschärft (slice-024b): **STEP** schreibt jetzt auch **Treppen** als B-Rep — die **Stufen** als **analytische** OCC-Box-Solids je Stufe (`BRepPrimAPI_MakeBox` aus der geteilten Kern-Query `stairStepBoxes`; das flache Treppen-Netz ist eine **nicht-manifolde** Box-Union, daher rekonstruiert statt vernäht). Das **Geländer** (render-only, Heuristik-Dicke) ist **ausgelassen** (nur im STL — explizit benannt). Damit sind **alle 3D-Bauteile B-Rep**; die 020b-STL-only-Lücke ist geschlossen | slice-024b ([ADR-0014](../docs/plan/adr/0014-step-stl-export-backend.md)) |
| 2026-07-01 | §1 `LH-FA-IO-007.a` neu (PDF-/PNG-Export-Mapping, [ADR-0016](../docs/plan/adr/0016-pdf-png-backend.md)): **Export-only, 2D-Achsen-Maßstabsplan**; selbst getragene Writer **io-resident** (`adapters/io/`, **kein OCC, kein Qt**; Vektor-PDF Seitenbaum+Content-Stream / Raster-PNG Chunks mit unkomprimierten DEFLATE-Blöcken + Adler-32/CRC-32); gerade Wand-Achsen je Geschoss (Datenquelle wie DXF), PDF **maßstäblich** (Modell-mm → Seiten-Einheit über dokumentierten Maßstab, `ACC-004`-beobachtbar), konkreter Maßstab/Seitenformat/DPI = Impl-Slice; **export-only** (kein Import-Adapter; Import-Request → generisches `E-IO-003`) / atomarer Export (`E-IO-001`, binär-sicher); Subset-Lücke (Footprint/Dicke/Räume/Bemaßung/Schraffur/Text/3D)/Totalität. **§6** zwei neue PDF/PNG-Vertragszeilen, **§7** PDF/PNG aus der Offene-Backends-Klausel entfernt (**alle IO-Backends entschieden**), **§4** `E-IO-001`-Bedingung um PDF/PNG-Export erweitert — `ADR-0016`-Folgepflicht | slice-025a |
| 2026-07-03 | §1 `LH-FA-PLG-001.a` neu (Plugin-System-Mapping, [ADR-0017](../docs/plan/adr/0017-plugin-api-abi.md)): Plugin-Host als **Driving Adapter** (`adapters/plugin/`), lädt Shared Libraries (`REQ-TEC-008`) über den System-Lader (Plugins laden nichts nach); **Vertrags-Handshake** versionierte Eintrittspunkte, **exakte** Versions-Gleichheit, fail-closed (Mismatch/fehlendes Symbol/nicht ladbar → Ablehnung **ohne** Initialisierung); **Lifecycle** Entdeckt→Geladen→Handshake→Initialisiert→Aktiv→Beendet→Entladen, jeder Fehlerpfad → Isolieren/Entladen bei unverändertem Modell, Kontext-Invalidierung vor Entladen; **Port-Vermittlung** nur Driving-Ports (kein Driven-Port, kein Beobachter-Zugang — pull-only in Hooks; Port-Subset v1 = Impl-Slice); **Threading** Hooks synchron im Hauptthread; **Fehler-Barriere** je Host→Plugin-Übergang (Unload-Strategie im Fehlerpfad = Impl-Slice); **Sandbox-Grenze ehrlich** (kein Speicherschutz in-process: Crash → `LH-QA-005`-Netz, Silent-Corruption + Speichern → kein Schutz). **§4** `E-PLG-001` präzisiert: **ein Code, zwei Log-Events** — `event=plugin_rejected` (Load/Handshake) vs. `event=plugin_error` (Laufzeit); Muster generische `E-IO-003`-Wiederverwendung, kein zweiter Code. **§5** neue Span-Zeile `bcad.plugin.lifecycle` (`plugin_id`, `op` load/unload/error, `result`). **§6** neue Plugin-API-Vertragszeile (versionierter Vertrag, Driving-Port-Vermittlung) — `ADR-0017`-Folgepflicht »AK-Schärfung + Spec-Nachzug« | slice-026a |
| 2026-07-03 | §1 `LH-FA-PLG-001.a` — die zwei an den Impl-Slice delegierten Punkte fixiert: **Port-Subset v1** = `EditStructurePort` (editierend) + `EvaluatePort` (lesend), weitere Ports additiv per Vertragsstands-Erhöhung; **Unload-Strategie im Fehlerpfad** = Isolieren **ohne** Entladen (Kontext-Entzug + barriere-gesicherte Instanz-Freigabe, Bibliothek bleibt bewusst im Prozess — Entlade-Restrisiken), nur der reguläre Weg entlädt vollständig. Impl-Beleg: realer Plugin-Host (`dlopen`-Monopol `adapters/plugin/`), Symbol-Naht = Executable-Symbol-Export (Kern bleibt statisch) | slice-026b ([ADR-0017](../docs/plan/adr/0017-plugin-api-abi.md)) |
| 2026-07-05 | §1 `LH-FA-DRW-005.a` neu (2D-Zeichen-Daten-Mapping, [ADR-0018](../docs/plan/adr/0018-drw-2d-zeichen-daten.md)): Datenheimat = pure Werttypen im Kern auf `Building` (Material-Präzedenz); Zugriffs-Naht = **neuer Driving-Port fürs 2D-Zeichnen** + Service (nicht `EditStructurePort`; Muster `EvaluatePort`), kein neuer `op`, `ViewModelPort` bleibt 3D-only; Hilfslinie = 2D-Segment auf Geschoss + Ebene, Ebene benannt/sichtbar/gesperrt/Farbe projekt-eindeutig, Zuordnung = direkter typisierter Bezug (nicht `entity_layers`); Layer-Sichtbarkeit = **Export-Filter**, DXF-Geschoss-`LAYER` unverändert, **Round-Trip-Asymmetrie** (Benutzer-Ebene nur nativ, nicht durch DXF); Beobachtbarkeit über Persistenz-Round-Trip + Export (kein interaktiver Canvas, deferiert). **§2.2** welle-5-DRW-Tabellen-Prosa (`layers` real, `guide_lines` neu, `entity_layers`/`documents` forward-deklariert). **§4** `E-VAL-001` um die **Ablehnungs-Lesart** erweitert (strukturell ungültige Mutation → hart abgelehnt, neben Klemmung; entartete Hilfslinie/leerer Ebenen-Name/Löschen referenzierter Ebene). **§6** interne Grenz-Vertragszeile 2D-Zeichnen (DRW). **Subset-Grenze** DXF/PDF/PNG autorisiert um Hilfslinien erweitert — `ADR-0018`-Folgepflicht »AK-Schärfung + Spec-Nachzug« | slice-032a |
| 2026-07-22 | §1 `LH-FA-DRW-005.a` + §2.2 auf **Impl-Stand** nachgezogen: Ebene und Hilfslinie **durabel** (SQLite-Round-Trip aktiv, ID-/feld-erhaltend; `layers` erstmals real verdrahtet, `guide_lines`-Tabelle ergänzt — FK `layer_id` `restrict` [Löschschutz], `storey_id`/`project_id` `cascade`, Insert-Reihenfolge Ebenen vor Hilfslinien / Hilfslinien nach Geschossen); `locked` **daten-durabel, nicht verhaltenswirksam** (kein Editier-Pfad in dieser Stufe — Lücke); Export-Sichtbarkeit (Hilfslinie im Grundriss, Ebenen-Sichtbarkeit als Export-Filter) auf den Export-Slice deferiert. Neues `make schema-regen`-Target für die deterministische `schema.sql`-Regeneration (verifiziert durch `schema-check`) | slice-032b ([ADR-0018](../docs/plan/adr/0018-drw-2d-zeichen-daten.md)) |
| 2026-07-22 | §1 `LH-FA-DRW-005.a` **Export-Sichtbarkeit aktiv**: eine Hilfslinie auf **sichtbarer** Ebene wird in den 2D-Grundriss-Export gezeichnet (DXF `LINE` auf dem Geschoss-`LAYER`, PDF/PNG als schlichtes 2D-Segment), eine **unsichtbare** Ebene wirkt als **Export-Filter** (ihre Hilfslinien fehlen im Artefakt). Der Sichtbarkeits-Filter ist **eine geteilte, format-agnostische Quelle** (PDF/PNG über die gemeinsame Plan-Projektion inkl. Bounding-Box-Erweiterung um sichtbare Hilfslinien, DXF über denselben Filter im Adapter) — kein Format-Drift; der Geschoss-`LAYER` bleibt unverändert (Benutzer-Layer = reiner Filter). Decode-Orakel je Format (Erscheint/Fehlt). Schließt die Export-Hälfte der DRW-005-AK (Fundament-Sequenz 032a→b→c) — `ADR-0018`-Folgepflicht »Export-Sichtbarkeit« | slice-032c ([ADR-0018](../docs/plan/adr/0018-drw-2d-zeichen-daten.md)) |
| 2026-07-23 | §1 `LH-FA-DRW-005.a` um die **interaktive 2D-Zeichenfläche (Canvas-Naht)** ergänzt: Zeichenfläche = Driving-Adapter (GUI-`view/`-Widget, neben dem 3D-Viewer); **2D-Lese-Naht** = reine 2D-Grundriss-Projektion in den Kern gehoben + neuer Driving-Read-Port (2D-Analog zu `ViewModelPort`, **eine Quelle** für Bildschirm + Export; die 2D-Exporter beziehen die Projektion vom Kern, kein Adapter leitet 2D-Geometrie ab), Regel B gewahrt (GUI zieht aus dem Kern), **vorgelagerter Refactor-Slice** (reiner Umzug, Decode-Orakel als Netz); invertierbare Bildschirm→Modell-Naht (display-frei testbar); **Selbst-Refresh ohne `op`** (»kein op« unrevidiert; Grenze: un-benachrichtigte Fremd-Mutation → Re-Eval); Kommando/Read über GUI-`command/`; Fang/Raster/Winkel = UI-Aids (nicht persistiert, eigene Slices). **§6** interne Grenz-Vertragszeile 2D-Zeichenfläche (DRW-Canvas). Beobachtbarkeit »ohne Canvas« → interaktiver Erzeugungs-Nutzerweg. [ADR-0019](../docs/plan/adr/0019-drw-2d-canvas.md)-Folgepflicht »AK-Schärfung + Spec-Nachzug«; die 2D-Lese-Naht wird kern-geliefert unter [ADR-0020](../docs/plan/adr/0020-driven-adapter-serialisieren-kern-liefert-geometrie.md) | slice-041a ([ADR-0019](../docs/plan/adr/0019-drw-2d-canvas.md)) |
| 2026-07-23 | §1 `LH-FA-IO-005.a` „Schicht" um die **Kern-Naht** (Geometrie-Bereitstellung) ergänzt: driven Export-Adapter serialisieren nur; der Kern liefert die abgeleitete Geometrie als pures `DerivedGeometry`-Bündel über `ModelExporterPort::write(Building, DerivedGeometry, path)` (pre-OCC-Primitive, format-selektiv; OCC-Montage bleibt adapter-resident, **kein** `TopoDS` über den neutralen Port); **staged** (Vertrag jetzt = accept-and-ignore, Kern-Berechnung + Adapter-Konsum mit der STEP/STL-Body-Migration). Die zwei adapter-genutzten puren Werte (`StepBox`, Mesh-z-Verschiebung) → `model/`-Kern | slice-042a ([ADR-0020](../docs/plan/adr/0020-driven-adapter-serialisieren-kern-liefert-geometrie.md)) |
| 2026-07-23 | §1 `LH-FA-IO-007.a` „Encoding & Schicht" um die **2D-Projektions-Bereitstellung** (Kern-Naht): `projectPlan`/`PlanView` in den Kern gehoben (`services/geometry/plan_projection` bzw. `model/plan_view`), PDF/PNG beziehen die `PlanView` kern-berechnet im `DerivedGeometry`-Bündel (Writer serialisieren nur); neuer `PlanViewPort` (2D-Lese-Naht, 2D-Analog zu `ViewModelPort`) speist Canvas + Export aus **einer** Quelle; `visibleLayerIds` als reiner Filter nach `model/` (geteilt Kern + io-DXF, kein Adapter→Kernel, keine `io→services_geo`-Kante), DXF unberührt; das io-`plan_geometry` entfernt. §1 `LH-FA-DRW-005.a` 2D-Lese-Naht auf „inzwischen realisiert" nachgezogen | slice-042b ([ADR-0019](../docs/plan/adr/0019-drw-2d-canvas.md), [ADR-0020](../docs/plan/adr/0020-driven-adapter-serialisieren-kern-liefert-geometrie.md)) |
| 2026-07-23 | §1 `LH-FA-IO-005.a` „Geometrie-Bereitstellung" von „staged" auf **realisiert**: der `ExchangeService` berechnet die 3D-Bauteil-Ableitung (pre-OCC-Primitive: Wand/Decke `{Footprint, Höhe/Dicke, Schnitt-Prismen, baseZ}`, Dach/Treppe als Netze/Box-Solids) format-selektiv für STEP/STL und reicht sie im `DerivedGeometry`-Bündel; STEP baut das B-Rep (`occ_solids`), STL tesselliert über den `GeometryKernelPort` — die reine Ableitung ist **kern-total**, den fail-closed-Skip degenerierter Bauteile besorgt der Adapter **beim OCC-Bau** (Regel C); die Geschoss-Höhen-Auflösung liegt als **eine** Wahrheit im Service. Die Schicht-Kante `geometry → services_geo` entfällt (architecture §2-geometry-Zeile mitgezogen) | slice-042c ([ADR-0020](../docs/plan/adr/0020-driven-adapter-serialisieren-kern-liefert-geometrie.md), [ADR-0014](../docs/plan/adr/0014-step-stl-export-backend.md)) |
| 2026-07-23 | §1 „Steigung abgeleitet" um die **Persistenz-Kern-Naht**: die write-derived `rise` berechnet der SQLite-Adapter nicht mehr selbst — sie wird ihm (wie die Export-Geometrie den Export-Adaptern) als **kern-gelieferter Skalar** über den Speicher-Vertrag gereicht (`PersistedDerivations`, `map<StairId,double>`); der Adapter **serialisiert nur** und bindet `rise_mm` (Laden liest ihn nicht zurück — Kern re-derived). Die Geschoss-Höhen-Auflösung ist als **ein** geteilter `optional`-Helfer konsolidiert (Export nutzt Default, Save wirft neutral bei danglendem `from_storey` — kein Teil-Speichern). Die Schicht-Kante `persistence → services_geo` entfällt (architecture §2-persistence-Zeile mitgezogen) → **alle** Adapter→Kern-Kanten weg | slice-042d ([ADR-0020](../docs/plan/adr/0020-driven-adapter-serialisieren-kern-liefert-geometrie.md), [ADR-0003](../docs/plan/adr/0003-persistenz-sqlite.md)) |
