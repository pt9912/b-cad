# MR-006 Plan-Review — slice-017g (MAT-006 Kosten-Auswertung)

**Review-Art:** Plan-Review (vor Implementierung, modul-10)
**Datum:** 2026-06-16
**Reviewer:** unabhängiger Agent (ohne Autoren-Kontext), `MR-006`-Linse
**Gegenstand:** [`docs/plan/planning/done-archive/slice-017g-kosten-auswertung.md`](../plan/planning/done-archive/slice-017g-kosten-auswertung.md)
**Verdict:** **0 HIGH — startbar** (2 LOW Politur, bei der Umsetzung).

## Findings & Dispositionen

| # | Kat | Quelle | Pfad | Befund | Disposition |
|---|---|---|---|---|---|
| L1 | LOW | IWYU | `material_line.h` | Nach `std::optional<double> cost` nutzt der Header `std::optional` direkt, zieht es aber transitiv über `material.h` → direkter `#include <optional>` (Projekt-Muster). | **Bei Umsetzung:** `<optional>` direkt einbinden. |
| L2 | LOW | spez. §1 | `spezifikation.md:434-446` | Der Kosten-Satz soll in den bestehenden Listen-Block eingewoben werden (Menge=Netto-Volumen bleibt eindeutig; `cost_per_m2`-Lücke neben die bestehenden Dach-/stairs-/openings-Lücken). | **Bei Umsetzung:** als Zusatz im selben §1-Absatz. |

## Negativbefunde (geprüft, ohne Befund)

- **Lastenheft MAT-006-AK** (`lastenheft.md:622-631`): „Kostenkennwerte (je m² **bzw.** je m³) … fließen je zugewiesenem Bauteil in die Summe ein". „bzw." = entweder/oder → Volumen-only (`cost_per_m3`) liegt im AK-Rahmen. Plan-Design treu, kein Über-Versprechen.
- **Lastenheft unberührt / `MR-010`:** MAT-006 seit 017a auf AK; Header 0.1.7 == oberste Historie-Zeile; kein Bump. Korrekt.
- **`Material`-Felder** (`material.h:22-23`): `cost_per_m2`/`cost_per_m3` als `optional<double>` vorhanden → Kosten ableitbar.
- **Additive Struct-Erweiterung sicher:** **Null** positionale Aggregat-Inits von `MaterialLine`/`MaterialReport` im Repo (Service nutzt `groups[id]` + Member-Zuweisung; `MaterialReport report;`); kein GUI/Adapter konsumiert den Report → neue Felder brechen 017f-AK nicht.
- **„Bestehende Methode erweitern, kein neuer Pfad":** `materialList()`-Schluss-Schleife ist der natürliche Ort (`line.cost`/`total_cost`), gleiche Iteration, read-only. (Det.: `cost_per_m3` lebt an `line.material.cost_per_m3`.)
- **nullopt-vs-0 (Korrektheit):** Material ohne `cost_per_m3` → `cost = nullopt` (nicht 0) = richtige Semantik („kein Kennwert" ≠ „kostenlos"), spiegelt den §1-NULL-Sicher-Präzedenzfall (017e); im AK gepinnt.
- **`cost_per_m2`-Lücke:** EVL-004 führt Volumen → Flächen-Kosten brauchen Flächen-Menge; Zurückstellung ehrlich, in spez. §1 (nicht Lastenheft). Honest.
- **Sizing/`ADR-0012`:** read-only-Aggregation, kein neuer Port/Werttyp, keine Geometrie → `MR-009` n/a, kein Code-Review. Kosten in `MaterialReport` (statt paralleler Methode) vermeidet eine zweite Gruppierung. Sound.
- **Regression:** bestehende 017f-AK prüfen nur name/count/quantity/total/Größen → additive Default-Felder bleiben textlich grün.

## Startfreigabe

Keine HIGH. L1/L2 (Politur) bei der Umsetzung. **slice-017g ist startbar.**
