# Review-Report: slice-013b (Plan) — 2026-06-13

**Review-Art:** Plan (`MR-006`) — geprüft gegen `ADR-0011`, Spec/Lastenheft,
**realen Code**, slice-012-Präzedenz, AGENTS §2.

**Gegenstand:** slice-013b (Türen + Fenster implementieren —
Wandöffnung), vor Implementierungs-Start.

**Reviewer:** unabhängiger AI-Agent (Reviewer ≠ Autor, kein
Autoren-Kontext). **Datum:** 2026-06-13.

---

## Gesamt-Verdikt

**Start frei nach MED-Einarbeitung. Keine HIGH.** Die Hauptsorge —
Persistenz-Lücke als Datenverlust-Verstoß (AGENTS §2.2) — **entkräftet
sich am realen Code (M1):** es gibt keinen lebenden Speicherpfad
(`SqliteProjectRepository` nicht in `main.cpp` verdrahtet, `Building`
ohne `openings`-Feld, kein Save-Use-Case im Driving-Port-Satz). „Speichern
lässt Öffnungen fallen" ist in welle-2 **kein beobachtbares Verhalten** →
der 013b/013c-Split ist sachlich sauber. Call-Site-Zählung „Service x4"
exakt verifiziert (`structure_edit_service.cpp:119,156,187,258` +
`:225`); `ViewerScene` trägt `WallGeometryChanged` real
(`viewer_scene.cpp:27`); Schema trägt openings/doors/windows.

## Findings (eingearbeitet)

- **M1 (MEDIUM) — Persistenz-Lücke korrekt, aber Closure-Auflage.** Kein
  §2.2-Verstoß (kein lebender Save-Pfad). **Auflage:** Closure benennt
  die Lücke explizit *und* hält fest, dass **slice-013c vor dem ersten
  lebenden Save-Use-Case** (Persistenz-Verdrahtung in `main`)
  abgeschlossen sein muss. **Eingearbeitet** (§6 + DoD-Closure-Zeile).
- **M2 (MEDIUM) — Split (i)/(ii) als geplante Zwei-Commit-Sequenz**, nicht
  als Notfall. **Eingearbeitet** (Schnitt-Herkunft + §6).
- **M3 (MEDIUM) — `ADR-0011`-Folgepflicht-Index zweigeteilt halten**
  (Geometrie/Verhalten erfüllt; Persistenz offen → 013c), nicht pauschal
  „erfüllt". **Eingearbeitet** (DoD-Schluss bereits so; bei Closure
  beachten).
- **M4 (MEDIUM) — ausgelassene Schema-Felder benennen**
  (`swing_angle_deg`, `is_external`, `frame_material`, `glazing_type`,
  `u_value`) als bewusster welle-2-Scope (Re-Eval-Trigger eigenes
  Solid/Persistenz). **Eingearbeitet** (§6).
- **L1 (LOW)** — `ViewerScene` deckt `WallGeometryChanged` vollständig
  (inkl. Eigen-Op); erwartet keine Code-Änderung, nur AK-Test. Plan sagt
  das bereits.
- **L2 (LOW)** — Default-Maße bei Anlage → §3-Konstanten (Präzedenz
  `kDefaultWallThicknessMm`), nicht ad hoc. Plan benennt das bereits.

## Bestätigt sauber

Lösungsfreiheit der Ebenen (Lastenheft 013a unberührt, Mechanik in
Spec/ADR); Transaktions-/Totalitäts-AK (Cut-Wurf → Modell unverändert,
Muster slice-012 W3-P2); zweigeteilte Regressions-Aussage (unberührte
Tests textlich grün / migrierte mit identischen Leer-Cutout-Orakeln,
Lehre slice-012 W3-P1); arch-check Regel C (`BRepAlgoAPI_Cut` nur im
Adapter, kein OCC-Leck); Port-Signatur = `ADR-0001`-Kern-Hoheit (keine
neue ADR).

**Zusammenfassung:** Solider, code-geerdeter Plan. Kein HIGH → Start
nicht blockiert; M1–M4 als Closure-Auflagen/Split-Disziplin eingearbeitet.
