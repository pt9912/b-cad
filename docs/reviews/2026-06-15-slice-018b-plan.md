# MR-006 Plan-Review — slice-018b (Voll-Korpus-`ids`, alle 7 ID-Familien)

**Datum:** 2026-06-15. **Reviewer:** unabhängiger Agent (kein Autoren-Kontext),
verifiziert gegen reale Quellen (grep/Read, d-check-Source im Schwester-Repo,
read-only Probe-Läufe mit Restore).
**Gegenstand:** [slice-018b](../plan/planning/next/slice-018b-voll-korpus-ids.md).

## Verdikt: HIGH-Findings vorhanden: ja (2 HIGH) — **alle eingearbeitet**

Findings nachverifiziert (Probe mit korrigierter Regex: Live 718, normativer Korpus
480, Logs 238) und in den Plan eingearbeitet (§1 Mengen, §2 Taxonomie+Cross-Stratum,
§3 DoD, §7 Sizing).

## HIGH (eingearbeitet)

- **HIGH-1 — `LH-FA`-Regex verfehlt das `D3`-Modul.** `LH-(FA-[A-Z]+|QA)-\d+` matcht
  die Ziffer in `D3` nicht (`harness/conventions.md` „Hinweis D3"): `LH-FA-D3-001`
  → kein Treffer. **36** reale `LH-FA-D3-*`-Vorkommen fielen lautlos durch (weder
  verlinkt noch geflaggt). **Eingearbeitet:** Regex → `LH-(FA-[A-Z][A-Z0-9]*|QA)-\d+`
  (§2); D3 als Edge-Case in §3; Neu-Messung bestätigt 36 D3-Treffer.
- **HIGH-2 — Discovery-Zahlen nicht reproduzierbar.** Plan nannte „Live 673"; mit
  korrigierter Regex auf dem aktuellen Korpus real **718** (LH-FA 255, MR 96, ADR 181,
  ACC 57, LH-QA 46, OBJ 33, REQ-TEC 24, E-* 26). Δ = 36 D3 + Korpus-Drift seit
  018a-Closure/018b-Plan. **Eingearbeitet:** §1 neu erhoben mit zitierter Config;
  normativer Korpus 480, Logs 238.

## MEDIUM (eingearbeitet)

- **MED-1 — Cross-Stratum-Link-Pflicht unter-spezifiziert.** `E-*` (Target
  `spezifikation.md`) kommt 6× in `lastenheft.md`, 5× in `architecture.md` vor;
  `OBJ`/`ACC` (Target `lastenheft.md`) in `spezifikation.md`/`architecture.md`. Da
  `inTarget` nur die eigene Target-Datei befreit, müssen diese **kreuz-stratum**
  verlinkt werden. **Eingearbeitet:** §2 „Cross-Stratum"-Regel (Ziel = kanonische
  Definitionsdatei der Familie, unabhängig von der Fundstelle; matrix erlaubt spec→spec).
- **MED-2 — Sizing.** „Ein Slice" reproduziert das Volumen, das slice-018 splittete;
  der Diff über ~480 Vorkommen in ~45 Dateien braucht beobachtbare Struktur.
  **Eingearbeitet/entschieden (Projektinhaber, Option 1):** ein Slice mit
  **Commit-Trennung nach Korpus-Bereich** (§2.8; §7) — je Commit einzeln review-/restore-bar.
- **MED-3 — DoD-Edge-Case „nur die nackte" falsch unter `always`.** `checkIDsAlways`
  (`ids.go`) flaggt Code-Span-IDs **genauso**, außer in `exempt-paths`/Target/Linktext.
  Im normativen Korpus sind also **beide** Formen link-pflichtig. **Eingearbeitet:**
  §3 Edge-Case korrigiert.

## LOW (eingearbeitet)

- **LOW-1 — `spec/*-historie.md` fehlten in `exempt-paths`.** Da `exempt-paths` nur
  Inline-Code befreit, brauchen die Historie-Dateien **beides** (backtick **und**
  `exempt-paths`-Eintrag); sonst bleiben 13/14 nackte Prosa-IDs `id-unlinked`.
  **Eingearbeitet:** §3 `.d-check.yml`-Zeile um `"spec/*-historie.md"` ergänzt.
- **LOW-2 — `MR`-Self-Referenz** in `harness/conventions.md` (14 `inTarget`-freie
  Vorkommen) — vom Plan §7 bereits korrekt gedeckt. Kein Befund, nur Bestätigung.

## INFO

- **INFO-1 — d-check-Source-Behauptungen alle korrekt** (unabhängig verifiziert):
  `exempt-paths` nur in `checkIDsAlways` (`ids.go:95`), nicht in der Prosa-Prüfung
  (`:121`); `idOccurrenceExempt` (`:154`) prüft nie das Link-Ziel → Datei-Link-Fallback
  bestätigt; `Slugify` (`anchors.go:60-74`) behält `_`, Duplikat-Suffix `-N`; `anchors`
  nur ATX-Slugs (HTML-Anker abgelehnt).
- **INFO-2 — Anker-Strategie B robust:** `OBJ`→`## 3. Projektziele`, `ACC`→`## 7.
  Abnahmekriterien`, `E-*`→`## 4. Fehler-Codes`, `REQ-TEC`→`## 9. …`; LH-FA/LH-QA mit
  eigenem Heading. Keine verwaiste ID gefunden.
- **INFO-3 — CHANGELOG-Backtick-Konversion bricht keine Links** (0 bestehende
  ID-Links in `CHANGELOG.md`).

## Negativbefunde (geprüft, ohne Befund)

- d-check-Source-Behauptungen (exempt-paths/always, Link-Ziel ungeprüft, Slugify,
  HTML-Anker): geprüft, ohne Befund (INFO-1).
- Taxonomie-Definitions-Orte OBJ/ACC/LH-QA/REQ-TEC/E-*: geprüft, ohne Befund (Lage
  korrekt; nur die LH-FA-Regex-Form war HIGH-1).
- matrix-Interaktion durch neue Familien-Links (spec→spec, results→spec): geprüft,
  ohne Befund (nur spec→adr/slice verboten).
- Anker-Robustheit, CHANGELOG-Link-Bruch, Self-Referenz: geprüft, ohne Befund.

## Fazit

HIGH-1 (D3-Regex) war ein konkreter Faktenfehler mit 36 lautlos unverlinkten IDs;
HIGH-2 untergrub die Sizing-Evidenz. Beide **behoben** (Regex-Fix + Neu-Messung).
MED-1/2/3 + LOW-1 in DoD/Strategie/Sizing **eingearbeitet**. Die d-check-Source-Analyse
des Plans war durchweg akkurat — die Mechanik-Grundlage stimmt. **slice-018b ist damit
startbar** (ein Slice, Commit-getrennt nach Korpus-Bereich).
