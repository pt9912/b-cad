# slice-031 — Dry-Run: misc-const-correctness + modernize-use-nodiscard (Beleg)

**Datum:** 2026-07-04. **Zweck:** aktuelle Fundstellen der zwei in slice-027
zurückgestellten Checks (Stand HEAD `7884964`, nach slice-027). Ablage unter
`docs/reviews/` = benannte Ausnahme (Evidenz, `ids`-exempt).

## Reproduktion

Gepinnte Toolchain (clang-tidy 21.1.8, [ADR-0004](../plan/adr/0004-toolchain-dependency-pinning.md)),
`bcad:build`, Scope `src/`+`plugins/` (43 `.cpp`), `HeaderFilterRegex '^src/'`:

```
docker run --rm bcad:build clang-tidy -p build \
  --extra-arg=--gcc-install-dir=/usr/lib/gcc/x86_64-linux-gnu/15 \
  -checks='-*,misc-const-correctness,modernize-use-nodiscard' \
  $(find src plugins -name '*.cpp')
```

Ergebnis: **misc-const-correctness 13, modernize-use-nodiscard 10** (Baseline
slice-027-Matrix: 14 / 10 — die const-Differenz folgt aus slice-027-Fixes).

> **Warnung (MR-006-MED-2):** Diese Zahlen gelten NUR für die **gebackene**
> `bcad:build`-Quelle mit gültiger `compile_commands.json` (`-p build`, **kein**
> Bind-Mount). Ein Lauf mit `-v $PWD:/src` auf einem Host **ohne** `build/` fällt
> auf geratene Compiler-Flags zurück und meldet still eine **falsche, disjunkte**
> Fundmenge (empirisch: const **37** statt 13). Sollwerte gegen den Gate: **13 / 10.**

## misc-const-correctness (13) — alle aktiviert + gefixt

| Fundstelle | Variable / Typ | Disposition |
|---|---|---|
| adapters/geometry/occ_geometry_adapter.cpp:89 | `mesher` `BRepMesh_IncrementalMesh` | `const` |
| adapters/geometry/occ_solids.cpp:74/95/149 | `face` `BRepBuilderAPI_MakeFace` (×3) | `const` |
| adapters/geometry/step_export_adapter.cpp:57 | `builder` `BRep_Builder` | `const` |
| adapters/io/dxf_import_adapter.cpp:134 | `in` `std::ifstream` | `const` |
| adapters/io/ifc_import_adapter.cpp:372 | `in` `std::ifstream` | `const` |
| hexagon/services/structure_edit_service.cpp:536/560/579 | `target` `model::Opening&` (×3) | `const&` (nur gelesen; Mutation läuft über `trial`-Kopie) |
| main.cpp:121 | `app` `QApplication` | `const` (`exec()` ist statisch) |
| main.cpp:125 | `geometry` `OccGeometryAdapter` | `const` (Service nimmt `const GeometryKernelPort&`) |
| main.cpp:131 | `mesh_source` `ViewModelMeshSource` | `const` |

Jeder Fund ist von clang-tidy als **const-kompilierbar** verifiziert (flaggt nur
bei durchgängig const-kompatibler Verwendung); Verhaltensneutralität durch die
Test-Suite abgesichert. Ein etwaiger false-positive (OCC-Lazy-`mutable`/Stream-
Zustand) würde per **Alternativ-Fix** oder — falls nötig — per begründetem
Wieder-Deaktivieren des **ganzen** Checks aufgelöst (`.clang-tidy`-Check-Exklusion
ist **global**, es gibt keine per-Fundstelle-Ausnahme); nie per NOLINT
([AGENTS.md §2.4](../../AGENTS.md)). Bei den 13 Fällen nicht erwartet.

## modernize-use-nodiscard (10) — alle aktiviert + gefixt

| Fundstelle | Methode | Disposition |
|---|---|---|
| hexagon/services/room_detection.cpp:75 | `intersectionWith` const | `[[nodiscard]]` |
| hexagon/services/room_detection.cpp:309 | `isInside` const | `[[nodiscard]]` |
| adapters/io/ifc_spf_reader.cpp:21/22/24 | `eof`/`peek`/`pos` const | `[[nodiscard]]` |
| adapters/persistence/sqlite_project_repository.cpp:95/130 | `handle`/`get` const | `[[nodiscard]]` |
| plugins/example/example_plugin.cpp:21 | `name` const (override) | `[[nodiscard]]` |
| plugins/testing/throwing_plugin.cpp:18 | `name` const (override) | `[[nodiscard]]` |
| plugins/testing/unload_throwing_plugin.cpp:18 | `name` const (override) | `[[nodiscard]]` |

**Override-Konsistenz:** die Basis `src/plugin_api/plugin.h` `virtual std::string
name() const = 0` erhält `[[nodiscard]]` mit (ABI-neutral — Compile-Attribut, kein
vtable-/Signatur-Einfluss), damit Basis und Overrides nicht auseinanderlaufen.

**Bilanz:** 23 Befunde (13+10) aktiviert + gefixt, 0 re-exkludiert (erwartet;
finale Bestätigung durch `make lint`).
