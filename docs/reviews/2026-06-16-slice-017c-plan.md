# MR-006 Plan-Review — slice-017c (EVL-002 Volumenberechnung)

**Datum:** 2026-06-16
**Reviewer:** unabhängiger Agent (ohne Autoren-Kontext), `MR-006`-Linse
**Gegenstand:** [`docs/plan/planning/in-progress/slice-017c-evl-volumen.md`](../plan/planning/in-progress/slice-017c-evl-volumen.md)
**Verdict:** 1 HIGH + 3 MED + 2 LOW + 2 INFO — **HIGH-1 eingearbeitet → startbar**

Das Review verifizierte jede ID, jeden Pfad, jede Formel und jede
Vertrags-Behauptung des Plans gegen die kanonischen Quellen und den realen Code
(Lastenheft EVL-002, spez. §1 EVL-001.a, `ADR-0012`, `structure_edit_service`,
`stair_geometry`, `slab_geometry`, `opening_geometry`, `wall_footprint`,
Modell-Header, `harness/conventions.md` `MR-006`/008/010/011, AGENTS §2.5).

## Verifizierte Kern-Claims (korrekt)

- **`ADR-0012` #4 Adapter-Volumen-Falle:** `solids_[id]` wird aus
  `buildWallSolid → geometry_.extrudeFootprint(...)` (OCC-Adapter,
  `GeometryKernelPort`) befüllt; `model::Solid` trägt `volume_mm3`. EVL-002 darf
  es **nicht** lesen → eigenständige Kern-Analytik. **Verifiziert true.**
- **Treppen-Geschlossen­form** `tread·width·h·(step_count+1)/2` = Σ Stufenkörper
  aus `stairMesh` (z ∈ [0,(i+1)·rise], rise=h/n). **Algebra exakt.**
- **Wand-Öffnungs-Klemmung** `clamped_height = min(sill+height, Wandhöhe) −
  max(0,sill)`; Öffnung schneidet die **volle Wandstärke** (kein eigenes
  thickness-Feld). **Verifiziert.**
- **Slab-Formel** `(Fläche − Σ Ausschnitt)·Dicke` + `cutoutInsideSlab`-Gate.
  **Verifiziert.**
- **`MR-008`/010/011-Hygiene:** Lastenheft unberührt (AK schon auf Niveau;
  Dach-Mechanik = Lösung → §1) → kein `MR-010`-Header-Bump; Historie ausgelagert
  (`spezifikation-historie.md`, §8 Pointer-Stub). **Verifiziert.**

## Findings & Dispositionen

| # | Sev | Befund | Disposition |
|---|---|---|---|
| HIGH-1 | HIGH | spez. §1 sagt heute „Je Bauteil: **Dach**/Platte/Treppe aus ihrem analytischen Solid". Der Plan ergänzte nur eine Dach-Lücke-Notiz → §1 widerspräche sich (Dach gleichzeitig „aus Solid" UND ausgeschlossen). | **Eingearbeitet:** DoD verlangt jetzt explizit, „Dach" aus der Aufzählung zu **entfernen** (→ „Platte/Treppe") + Lücke/Re-Eval ergänzen. |
| MED-1 | MED | „Geländer 50 mm hartkodiert, kein spez. Parameter" ist faktisch falsch: 50 mm = Streifen­stärke (`kRailThicknessMm`, Render); die Geländer­*höhe* ist spez. (STR-004, `STAIR_RAILING_HEIGHT_MM`). | **Eingearbeitet:** Wortlaut in §1/§6 korrigiert — Geländer = generierte Render-Geometrie (Höhe spez., Streifenstärke unspez.-Render, kein Material). Entscheidung (Stufenkörper-only) bleibt; Test-Sentinel (`kRailThicknessMm`-Unabhängigkeit) bleibt. |
| MED-2 | MED | DoD plante einen `ADR-0012`-Body-Edit (Re-Eval-Trigger). ADRs sind nach Accepted immutabel (AGENTS §2.5); die bestehenden Trigger („Nicht-prismatische/komplexe Volumen", „Exaktes vereinigtes Volumen") tragen die Dach-Wiedervorlage ohnehin. | **Eingearbeitet:** `ADR-0012`-Edit **gestrichen**; Dach-Lücke lebt nur in spez. §1. Plan-Tabelle bereinigt. |
| MED-3 | MED | Vorzeichenlose Shoelace existiert bereits **zweimal** file-lokal (`structure_edit_service.cpp` `polygonArea`, `slab_geometry.cpp` `absPolygonArea`). | **Eingearbeitet:** Slice konsolidiert die **service-lokale** Kopie auf `volume_geometry::polygonArea` (hält die Zahl bei zwei); slab-Kopie als benannte Drift-Notiz für späteren Merge. |
| LOW-1 | LOW | `slab.cutouts` sind durch `addSlabCutout` vorgefiltert — ein Außen-Cutout kann gar nicht im Slab landen; der „kein Abzug"-Test prüfte sonst nichts. | **Eingearbeitet:** Test ruft die **pure** `slabNetVolumeMm3` direkt mit handgebautem Außen-Cutout. |
| LOW-2 | LOW | „Treppe → unbekanntes Geschoss → 0, kein Wurf" (storeyHeight 0) nur implizit. | **Eingearbeitet:** Boundary explizit gepinnt. |
| INFO-1 | INFO | `storeyHeight` ist private — ok, `volume()` ist Member. | keine Aktion |
| INFO-2 | INFO | Session-Scope angemessen (1 Werttyp + additive Port-Methode + 1 pure Geometrie-TU + 1 Service-Methode + 1 Testdatei + Doku); DoD-Zeilen sensor-/test-gedeckt; `MR-009` korrekt invoziert. | keine Aktion |

## Startfreigabe

HIGH-1 ist eingearbeitet (Quellen-Konsistenz-Defekt gelöst); MED-1/-2/-3 und
LOW-1/-2 ebenfalls. **slice-017c ist startbar.** `MR-009` (geometrielastiges
Code-Review) greift vor der Welle-Closure.
