# MR-006 Plan-Review — slice-019a (IFC-Import/-Export AK-Schärfung & Spec-Mapping)

**Review-Art:** `MR-006` — Unabhängiges Plan-Review **vor** Implementierungs-Start
(HIGH blockiert den Start). Gegenstand ist ein **Plan (Prosa), kein Diff** — es
gibt noch keine Implementierung.

**Gegenstand:** [`docs/plan/planning/done-archive/slice-019a-ifc-ak-spec.md`](../plan/planning/done-archive/slice-019a-ifc-ak-spec.md)
(Doku-/Entscheidungs-Slice: schärft `LH-FA-IO-001`/002 von Outline auf
AK-Niveau, fügt das IFC-Subset-Mapping in `spezifikation.md` §1 ein, zieht
§6/§7 als `ADR-0013`-Folgepflicht nach; parametrisiert auf `ADR-0013`, **kein
neuer ADR, kein Code**).

**Reviewer:** unabhängiger Agent (Reviewer ≠ Plan-Autor; getrennte Session,
ohne Autoren-Kontext über die Dokumente hinaus).

**Eingangs-Kontext (gelesen):**

- Der Plan selbst (vollständig).
- `docs/plan/adr/0013-ifc-bibliothek.md` (accepted 2026-06-16) — Entscheidung,
  Subset #4, Folgepflichten, Fitness Function.
- `spec/lastenheft.md` — Header `**Version:**` (Z. 3), `### Modul Import /
  Export (IO)` (Z. 693–713, `LH-FA-IO-001`..008), `ACC-003`/`ACC-004`
  (Z. 785–786), `OBJ-005` (Z. 44).
- `spec/lastenheft-historie.md` — oberste Zeile (0.1.7) + `MR-010`-Invariante.
- `spec/spezifikation.md` — §1 `LH-FA-ROF-001.a` (Format-Präzedenz, Z. 155 ff.),
  §4 Fehler-Codes (`E-IO-001`/002/003, Z. 610–620), §6 Externe Verträge
  (IFC-Zeile, Z. 651), §7 Offene Punkte (IFC-Zeile, Z. 659), Header (kein
  `**Version:**`, nur `**Letzte Änderung:** 2026-06-08`).
- `spec/spezifikation-historie.md` — dateless-versionierte Provenance (kein
  Versions-Header-Zwang).
- `spec/architecture.md` — Port-Tabelle (Z. 81/89/90), IO-Adapter-Zeile
  (Z. 105), Use-Case `LH-FA-IO-001 — IFC-Import` (Z. 206 ff.).
- `harness/conventions.md` — `MR-001`, `MR-006`, `MR-008`, `MR-010`, `MR-011`, `MR-012`.
- `docs/plan/adr/README.md` — ADR-Index-Zeile 0013 + Folgepflicht-Tabelle
  (Z. 17/41–43).
- `.d-check.yml` — `matrix`/`ids`-Scope (Frontmatter-Link-Frage).
- `docs/plan/planning/done-archive/slice-014a-dach-ak-spec.md` — Format-Präzedenz.
- `Makefile` — `docs-check`/`gates`/`schema-check`/`arch-check`-Targets.
- **Empirisch:** `make docs-check` auf dem aktuellen Tree (Plan liegt bereits in
  `in-progress/`) → **`d-check: 110 Datei(en) geprüft, 0 Befund(e)`, EXIT=0.**

---

## Findings

### LOW-1 — `spezifikation.md`-Header `**Letzte Änderung:**` nicht im Nachzug genannt

- **kategorie:** LOW
- **quelle:** Quellen-Konsistenz / Plan-Qualität (DoD-Beobachtbarkeit)
- **pfad:** Plan DoD-2/§3-Tabelle (Zeilen `spec/spezifikation.md`) ↔
  `spec/spezifikation.md:3` (`**Letzte Änderung:** 2026-06-08`)
- **befund:** Der Plan ändert §1/§6/§7 der `spezifikation.md`, nennt aber im
  DoD und in der Plan-Tabelle keinen Nachzug des Header-Felds
  `**Letzte Änderung:** 2026-06-08` auf das Bearbeitungsdatum; anders als beim
  Lastenheft (`**Version:**`, `MR-010`/012-gated) deckt **kein** Gate dieses Feld,
  es bliebe still stale.
- **verifizierbar:** ja (Feld existiert wörtlich, ist im Plan nicht erwähnt).

### LOW-2 — Neue Boundary-AK für IO-001 nicht als Outline→AK-Zuwachs ausgewiesen

- **kategorie:** LOW
- **quelle:** `MR-008` / Plan-Qualität
- **pfad:** Plan DoD-1 (Z. 60–62) ↔ `spec/lastenheft.md` (Z. 699–705)
- **befund:** Das bestehende `LH-FA-IO-001` führt nur **Happy + Negative**; die
  vom Plan geforderte **Boundary** („leere/strukturlose IFC-Datei → leeres/
  teil-leeres Modell ohne Absturz") ist eine neue AK-Zeile. Der Plan
  formuliert sie zwar lösungsfrei, weist sie aber nicht ausdrücklich als
  hinzukommend (vs. „bestehende AK erhalten") aus — die Trennung
  erhalten/neu ist im DoD-Text vermischt.
- **verifizierbar:** ja (Quell-AK hat keine Boundary; Plan-DoD fordert eine).

### LOW-3 — Sammelblock-ID `LH-FA-IO-001.a` deckt auch Export (IO-002), Benennung asymmetrisch

- **kategorie:** LOW
- **quelle:** Plan-Qualität (Format-Konsistenz)
- **pfad:** Plan DoD-2 (Z. 71–72) / §3-Tabelle (Z. 109)
- **befund:** Der geplante §1-Block heißt `LH-FA-IO-001.a`, deckt aber laut Plan
  **IO-001 (Import) + IO-002 (Export)**. Die Präzedenz `LH-FA-ROF-001.a`
  (`spezifikation.md:155`) deckt einen zusammenhängenden Nummern-Lauf
  (ROF-001..005) unter dem 001-Anker; hier wird der Export (IO-002) unter dem
  Import-Anker (IO-001) mitgeführt. Der Plan benennt das als bewussten
  „Sammelblock — deckt IO-001 + IO-002", die Asymmetrie ist also offen, aber die
  ID suggeriert allein Import.
- **verifizierbar:** ja (Plan-Text + Präzedenz nachlesbar).

### INFO-1 — Auto-verlinkte Frontmatter-IDs sind Tooling-Artefakt, kein Defekt

- **kategorie:** INFO
- **quelle:** Quellen-Konsistenz (`MR-011` / `ids`-Modul)
- **pfad:** Plan-Frontmatter `lastenheft_refs`/`adr_refs` (Z. 6–7) ↔ `.d-check.yml`
  `ids` (`scope.roots: ["."]`, `ignore: [done-archive]`, `link-policy: always`)
- **befund:** Die Frontmatter-IDs in slice-019a sind als Markdown-Links
  ausgeführt (anders als die nackten IDs in `done-archive/slice-014a`). Das ist
  **vom Gate erzwungen**: `ids` verlangt `link-policy: always` im gesamten Live-
  Korpus außer `done-archive` (eingefroren) und den exempt-Logs; `in-progress/`
  ist nicht exempt. Empirisch bestätigt: `make docs-check` ergibt **0 Befunde**
  auf dem Tree, der diesen Plan bereits enthält. **Kein** Plan-Problem.
- **verifizierbar:** ja (`.d-check.yml` + Gate-Lauf EXIT=0).

### INFO-2 — Split-Punkt korrekt gesetzt, Einzel-Sitzungs-Umfang plausibel

- **kategorie:** INFO
- **quelle:** Plan-Qualität (Schnitt/Sitzungs-Umfang, `MR-006` b)
- **pfad:** Plan §6 „Split-Punkt" (Z. 155–157)
- **befund:** Der benannte Split (DoD-1 Lastenheft-AK vorab schließbar, da
  unabhängig von der §1-Mechanik → DoD-2/3 als zweiter Schritt) ist sachlich
  korrekt: DoD-1 hängt nicht von der Mapping-Entscheidung ab. Der Gesamt-Umfang
  (reine Doku, ein Lastenheft-Modul + ein §1-Block + §6/§7-Streichung) ist als
  eine Sitzung review-bar; ein Zwangs-Split ist **nicht** erforderlich.
- **verifizierbar:** ja.

---

## Negativbefunde (geprüft, ohne Befund)

- **`MR-008` (Lösungsfreiheit Lastenheft):** geprüft, ohne Befund. DoD-1 verbietet
  ausdrücklich IFC-Entitätsnamen/Encoding/Mapping-Mechanik im Lastenheft-Text
  und verweist sie nach §1; die Teilumfang-Klausel ist benutzer-beobachtbar
  formuliert („Türen/Fenster/… werden übersprungen bzw. nicht geschrieben"),
  nicht über Entitäts-Listen. §6 des Plans wiederholt die Linse explizit.
- **`MR-010` / `MR-012` (Header == oberste Historie-Zeile):** geprüft, ohne Befund.
  `lastenheft.md` Header ist real **0.1.7** (Z. 3), oberste
  `lastenheft-historie.md`-Zeile ist **0.1.7** — Invariante hält aktuell; der
  geplante Bump auf **0.1.8** ist die korrekte nächste Version und zielt korrekt
  auf die oberste Historie-Zeile **und** den Header (beide Artefakte in der
  Plan-Tabelle).
- **Subset-Treue zu `ADR-0013`:** geprüft, ohne Befund. Der geplante §1-Block
  behauptet exakt das `ADR-0013`-#4-Subset (räumliche Struktur `IfcProject`→
  `IfcSite` optional →`IfcBuilding`→`IfcBuildingStorey` über `IfcRelAggregates`;
  gerade achsen-getragene Wände mit `IfcMaterialLayerSetUsage`); **Export IFC4
  `IfcWall`, nicht** das in IFC4 deprecated `IfcWallStandardCase`; Import
  akzeptiert beide Wand-Formen + IFC4/IFC2x3; atomarer Import → `E-IO-003`,
  kein Teil-Import; Totalität bei degenerierter Datei. **Kein Über-Versprechen**
  (Türen/Fenster/Dach/Decken/Treppen als benannte Lücke offen).
- **§6/§7-Nachzug (`ADR-0013`-Folgepflicht, Richtung):** geprüft, ohne Befund.
  §6-IFC-Zeile lautet real „Schema-Version offen (ADR-Folge)" (Z. 651), §7
  führt real „IFC-Schema-Version und -Bibliothek (ADR in welle-4-austausch)"
  (Z. 659) — beide Plan-Behauptungen stimmen. Der Nachzug ist die erlaubte
  ADR→Spec-„präzisiert"-Richtung (`MR-001`/`MR-011`), nicht erweiternd; die
  zugehörige offene Folgepflicht-Zeile existiert in
  `docs/plan/adr/README.md:41`.
- **DoD-Beobachtbarkeit / Sensor-Deckung:** geprüft, ohne Befund. Für den
  Doku-Slice ist `make docs-check`/`make gates` der reale Sensor (Link-/Anker-/
  ID-Integrität) — empirisch grün (0 Befunde). Die Lösungsfreiheits-Linse
  (`MR-008`) ist korrekt als **inferential** (`MR-006`-Review), **nicht** als Gate
  ausgewiesen — keine über-versprochene Sensor-Deckung. `make schema-check` ist
  ein realer Target (Makefile:104, „NICHT in gates"); der Plan nennt ihn korrekt
  als „unberührt (kein Schema-Bezug)".
- **Quellen-Konsistenz (IDs/Pfade/Fehler-Codes/Ports/Use-Case):** geprüft, ohne
  Befund. `E-IO-001` = „Kein Schreibrecht im Zielpfad" (korrekt für
  nicht-beschreibbares Export-Ziel — **nicht** `E-IO-002` = „Zielmedium voll");
  `E-IO-003` = „Import-Format nicht erkannt/invalide" (korrekt für nicht-IFC);
  `ACC-003`/`ACC-004` korrekt; Ports `ExchangeModelPort`/`ModelImporterPort`/
  `ModelExporterPort`/`ExchangeService` + `adapters/io/` existieren
  (architecture.md §1.1/§4); der Use-Case „`LH-FA-IO-001` — IFC-Import" existiert
  (architecture.md:206). Alle Anker-Ziele der Plan-Links lösen auf.
- **Format-Präzedenz (slice-014a-Muster):** geprüft, ohne Befund. Struktur
  (Ziel/DoD/Plan-Tabelle/Trigger/Closure-Trigger/Risiken/Sub-Area-Modus/
  Closure-Notiz-Platzhalter), Sammelblock-Idee, Teilumfang-Klausel und der
  „kein ADR/kein Code"-Charakter folgen der 014a-Vorlage; der Closure-Notiz-
  Platzhalter ist korrekt offen.

---

## Kategorie-Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 0 |
| MEDIUM | 0 |
| LOW | 3 |
| INFO | 2 |

---

## Verdikt

**Implementierungs-bereit.** **Keine HIGH** — der Start ist nicht blockiert. Alle
prüfbaren Vertrags-Behauptungen (Fehler-Codes, ACC-/OBJ-IDs, Ports, §4-Use-Case,
§6/§7-Ist-Stand, Header-Version 0.1.7→0.1.8, `ADR-0013`-Subset, Folgepflicht-Zeile)
sind gegen die kanonischen Quellen verifiziert und korrekt; `make docs-check`
läuft auf dem Tree mit dem Plan **grün (0 Befunde)**. Die drei LOW-Funde
(`Letzte Änderung`-Feld-Nachzug, Boundary-AK-Zuwachs explizit ausweisen,
Sammelblock-ID-Asymmetrie) sind Präzisierungen ohne Start-Block; der Implementer
entscheidet über ihre Aufnahme (`MR-006`: Reviewer kategorisiert, Implementer
entscheidet). Die auto-verlinkten Frontmatter-IDs sind ein vom `ids`-Gate
erzwungenes Tooling-Artefakt, **kein** Defekt (INFO-1).
