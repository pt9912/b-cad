# MR-006 Plan-Review — slice-018 (d-check ids+matrix scharf) → Split 018a/018b

**Datum:** 2026-06-14. **Reviewer:** unabhängiger Agent (kein Autoren-Kontext),
verifiziert gegen die realen Quellen (grep/Read).
**Gegenstand:** ursprünglicher slice-018-Plan (Doku-ID-Link-Pflicht +
Referenzmatrix + done-archive). **Ergebnis:** **3 HIGH + 3 MED + LOW** →
aufgeteilt in [`slice-018a`](../plan/planning/done-archive/slice-018a-dcheck-gate-mechanik-done-archive.md)
(Mechanik) + slice-018b (Korpus-Verlinkung + `ids`, deferred).

## Verdikt: HIGH-Findings vorhanden: ja (alle eingearbeitet/in den Split übernommen)

## HIGH (eingearbeitet)

- **HIGH-1 — ID-Taxonomie falsch (Autor-Faktenfehler):** Die „Definitions-Datei"-
  Greds nahmen die *erste* Treffer-Datei, nicht die *Definitions*-Datei. Real:
  `OBJ-001..005` sind in **`spec/lastenheft.md`** (Z. 40-44) definiert (nicht
  architecture); **alle** Fehlercodes `E-(IO|VAL|GEO)-*` im selben Katalog
  **`spec/spezifikation.md`** (nicht IO/VAL/GEO aufgeteilt). **Korrigiert** in der
  018a-Taxonomie-Notiz: `OBJ-`+`ACC-`→`lastenheft.md`; `E-*`+`REQ-TEC-`→
  `spezifikation.md`.
- **HIGH-2 — „matrix = `MR-006` computational" Over-Claim:** matrix flaggt nur die
  **5 Markdown-Link-Formen**, lässt die ~55 **nackten** Spec→ADR-Tokens im
  bindenden Text unberührt. **Eingearbeitet:** Anspruch zurückgestuft — matrix
  setzt die **Link-Richtung** durch (`↛ adr`, `↛ slice`, `forbidden superseded`);
  die nackten Text-Tokens bleiben gate-unsichtbar (Residual benannt).
- **HIGH-2-Folge — §2.7 vs. Regelwerk (durch Regelwerk-Prüfung aufgelöst):** Der
  Reviewer las §2.7 in `AGENTS.md` („`architecture.md` referenziert ADRs") als
  Erlaubnis und empfahl, `spec-straten ↛ adr` zu lockern. **Nachträgliche
  Regelwerk-Prüfung (Projektinhaber, 2026-06-14) hob das auf:** das Regelwerk
  (§Referenz-Richtung) sagt wörtlich „**Spec → ADR existiert im bindenden Text
  nicht — auch nicht als Quellen-Spalte**"; Spec→ADR steht **nicht** in der
  erlaubten Aufwärts-Liste. §2.7 ist damit eine **undeklarierte Inkonsistenz**
  (AGENTS rangiert #7 < Regelwerk), keine Erlaubnis. **Direktion (a) gewählt:**
  `matrix` setzt `spec-straten ↛ adr` durch; die 5 Link-Funde sind echte Verstöße
  → aufgelöst (`architecture.md` §6 ADR-Index + Inline-Links, `spezifikation.md`
  Z. 472); §2.7 reconcilet.
- **HIGH-3 — Exempt-Mechanik ungeklärter Blocker:** `exempt-paths` griff in der
  Sondierung nicht. Das done-archive-Scoping (von `ids` aus, von `links` weiter
  geprüft) hängt daran. **Eingearbeitet:** empirischer **Vor-Schritt** in 018a vor
  jeder Migration; Fallback (`scan.ignore`-Trade-off) benannt.

## MEDIUM (eingearbeitet)

- **MED-1 — Volumen:** real ~779 Live-Vorkommen + ~1136 in `done/` (nicht „1500
  Großteil done"). Stützt die done-archive-These; 018a/018b-Split dimensioniert.
- **MED-2 — Selbst-Referenz:** Definitionsdateien (`lastenheft.md` def. `LH-*`)
  würden unter `always` sich selbst link-pflichtig — harte 018b-Voraussetzung,
  an HIGH-3 (gleiche Mechanik) gekoppelt.
- **MED-3 — leere Refs:** Harness-Slice ohne LH/ADR vertretbar, aber an
  **`MR-006`/`MR-007`** binden (Commit-Hook-Regel) — in 018a-Bezug verankert.

## LOW / INFO (eingearbeitet)

- **LOW-1 — `*results.md`/acc-002:** 3 results + 4 acc-002-Artefakte bleiben in
  `done/` (lebende Referenz, ADR-quer-referenziert), nur die 31 slice/spike-Pläne
  wandern. **LOW-2 — link-policy:** Reviewer empfahl `prose`; **revidiert auf
  `always`** (Regelwerk „auch nicht als Quellen-Spalte" + Dogfooding-Linie —
  Inline-Code-IDs mit erfasst). **INFO-1 — Welle:** `harness-steering` Quergewerk
  vertretbar, Roadmap-Notiz. **INFO-2 — spans/hostpaths:** 0 Befunde, risikoarm.

## Schnitt-Empfehlung (übernommen)

**018a** = Archiv-Mechanik (done-archive + `links`-validierte Migration + `spans`/
`hostpaths`) **+ Referenz-Richtung Spec→ADR** (`matrix` Regelwerk-Richtung +
`ids` ADR-Muster spec-gescopt `always` + ~72-Ref-Spec-Sanierung + §2.7-Reconcile
+ Lifecycle/Gate-Doku). **018b** = Voll-Korpus-`ids` (alle 7 Familien, ~780
Verlinkungen, `always`). **Sizing:** 018a ist groß — Commit-Trennung (§2.8:
reiner Move / Inhalt / Spec-Sanierung); falls zu groß, die Spec→ADR-Richtung als
eigenen Slice (018a2) abspalten (`MR-006`). Grund-Split: `git mv` + Voll-Korpus-
Masseninhalt in einer Sitzung nicht beobachtbar reviewbar.

## Fazit

HIGH-1/2/3 blockierten den Einzel-Slice; mit Split + Taxonomie-Korrektur +
matrix-Lesart-nach-§2.7 + Exempt-Vor-Schritt ist **018a startbar**.
