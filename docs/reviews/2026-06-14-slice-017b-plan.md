# MR-006 Plan-Review — slice-017b (Auswertung Flächen)

**Datum:** 2026-06-14. **Reviewer:** unabhängiger Agent (kein Autoren-Kontext).
**Gegenstand:** [`slice-017b-evl-flaechen.md`](../plan/planning/done-archive/slice-017b-evl-flaechen.md)
(`EvaluatePort` + EVL-001 Grundfläche + EVL-003 Wohnfläche, Aggregation der
Raum-Netto-Flächen).

## Verdikt: HIGH-Findings vorhanden: nein

Quellen-treu und ADR-0012-konform. Die Kern-Prämisse (EVL-001/003 = Aggregation
der bereits in `room_detection` berechneten `Room.net_area_mm2`, kein Shoelace-
from-scratch) ist gegen `room.h:32`/`room_detection.cpp:390` verifiziert. Schnitt
bestätigt: **EVL-002 (Volumen) zu Recht nach 017c** (geometrie-schwer/MR-009),
EVL-001/003 sind reine Summen (MR-009 n/a). 2 MED + 2 LOW eingearbeitet.

## Verifiziert
- `Room.net_area_mm2` = Netto (äußerer Ring − Löcher), in `room_detection.cpp`
  berechnet (`room.h:24-32`). `StructureEditService` hält `rooms_`
  (`structure_edit_service.h`) und implementiert `DetectRoomsPort` — Muster für
  `EvaluatePort`. `EvaluatePort` ist in `architecture.md` §1.1 deklariert
  (017a-Nachzug). ADR-0012-Treue (read-only/kein `op`/kein `GeometryKernelPort`)
  gehalten. `make arch-check`/`make test` real (keine behaupteten Gates).

## MEDIUM (eingearbeitet)
- **MED-1 — EVL-001 „je Raum":** das Lastenheft-AK verlangt „Netto-Grundfläche
  **je Raum** (und die Summe)"; der `AreaReport` modellierte nur die Summe.
  **Eingearbeitet:** `AreaReport { total_m2; std::vector<double> room_areas_m2 }`
  (Per-Raum + Summe).
- **MED-2 — Sensor-Wortlaut:** „arch-check A–E" → reales Target **`make
  arch-check`**. Eingearbeitet.

## LOW (eingearbeitet)
- **LOW-1 — kein versteckter Re-Detect:** `EvaluatePort`-Methoden sind `const`
  (struktureller Schutz); AK belegt zusätzlich, dass das Ergebnis über zwei
  Abfragen identisch ist und `building()` unverändert bleibt.
- **LOW-2 — Loch-Ring-Test:** AK mit einem Raum mit innenliegendem Wandzug
  (Netto = äußerer Ring − Loch fließt durch) — belegt die ADR-0007-Netto-
  Wiederverwendung; Fallback: Netto-Reuse begründet dokumentieren, falls die
  Konstruktion keinen sauberen Loch-Ring erzeugt.

## INFO
- MR-010 n/a (017b schärft das Lastenheft nicht — EVL-001/003 in 017a gereift);
  MR-008 n/a (Impl-Slice). Brace-Notation der geplanten Dateien korrekt.

## Fazit
Keine HIGH → startbar. Schnitt richtig (Port + Werttyp + Aggregation; Volumen
017c). MED-1/2 + LOW-1/2 eingearbeitet.
