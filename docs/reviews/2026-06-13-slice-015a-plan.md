# Review-Report: slice-015a (Plan) — 2026-06-13

**Review-Art:** Plan (`MR-006`) — Decken+Fundament AK-Schärfung &
Spec-Geometrie, geprüft gegen Lastenheft/Spec, `slabs`-Schema, realen
Port, `ADR-0011`/0006/0002, `MR-008`, AGENTS §2.

**Reviewer:** unabhängiger AI-Agent (Reviewer ≠ Autor). **Datum:** 2026-06-13.

---

## Gesamt-Verdikt: **GO mit Auflagen** (kein Start-Blocker).

HIGH-1 ist eine **Formulierungs-Korrektur am Plan/an der zu schreibenden
Spec** (kein Scope-/ADR-Defekt) — vor dem §1-Schreiben eingearbeitet.
„Kein neuer ADR" durch `ADR-0011` #6 belegt (nennt SLB/FND wörtlich);
`slabs`-Schema trägt SLB **und** FND (slab_type, keine separate
foundations-Tabelle); Sammelblock-Benennung konventionskonform; Umfang
realistisch (gemeinsame Platten-Geometrie).

## Findings (eingearbeitet)

- **HIGH-1 (nicht-blockierend) — „kein neues Primitiv" zu stark.**
  `extrudeFootprint` extrudiert ab z=0; Decken/Fundament liegen auf einer
  Aufstandshöhe → ob der Port unverändert trägt oder um `base_z`
  erweitert wird, ist eine Port-Signatur-Frage (`ADR-0001`-Kern-Hoheit,
  Muster `ADR-0011` #2) und gehört zu 015b. **Eingearbeitet:** §Ziel/DoD
  auf „gleiches Geometrie-Vokabular, Port-Mechanik offen → 015b"
  abgeschwächt.
- **MED-2 — D3-001.a-Drift.** D3-001.a listet „Decken/Dach über den Port",
  aber das Dach (ROF-001.a) geht **analytisch** im Kern. **Eingearbeitet:**
  §1 SLB-001.a begründet die Platten-Extrusion eigenständig, nicht per
  D3-001.a-Analogie.
- **LOW-2 — `base_z`-Herkunft.** Das `slabs`-Schema trägt **kein** base_z.
  **Eingearbeitet:** §1 nennt die Herkunft je `slab_type` (Decke =
  Geschoss-OK, Bodenplatte = 0, Fundament = unter Gelände).
- **LOW-1 — `ADR-0002`-Scope.** `ADR-0002` liefert nur das Boolean-Backend
  für SLB-003-Ausschnitte, **nicht** die base_z-Erweiterung (= `ADR-0001`).
  **Eingearbeitet** (§1-Klarstellung).
- **MED-1 / `MR-008` — Lösungsfreiheit Lastenheft.** Mechanik-Vokabeln
  (Boolean, CutPrism, Port, base_z) nur in §1, **nie** im Lastenheft-Text;
  Maßstab ROF-001..005. **Auflage bei Umsetzung** (strikt prüfen).

## Bestätigt sauber (INFO)

- `ADR-0011` #6 deckt SLB/FND wörtlich → kein neuer ADR (wie Dach 014a).
- `slabs`-Tabelle trägt SLB+FND über `slab_type` (kein separates
  foundations-Schema) — Plan-Behauptung korrekt.
- Sensor-Deckung ehrlich (reine Doku → docs-check/gates + `MR-006`-Linse,
  keine behauptete Test-Deckung).
- FND-Semantik (Tiefe/unter Gelände) als Lastenheft-AK lösungsfrei
  fassbar; Mechanik in §1; `FOUNDATION/SLAB`-Konstanten in §3 noch
  anzulegen (Plan fordert es).

**Zusammenfassung:** Quellentreuer a-Slice im 014a-Muster; die einzige
substanzielle Auflage (HIGH-1) ist die ehrliche Verortung der
base_z-/Port-Frage bei 015b — eingearbeitet → Start frei.
