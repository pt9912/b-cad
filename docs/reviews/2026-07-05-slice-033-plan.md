# MR-006 Plan-Review — slice-033 (d-check v0.37.1 & Matrix `adr→slice`)

**Datum:** 2026-07-05 · **Reviewer:** unabhängiger Agent (≠ Autor), read-only ·
**Gegenstand:** `docs/plan/planning/done-archive/slice-033-dcheck-matrix-adr-slice.md`

**Verdikt: 0 HIGH — Start nicht blockiert.** Evidence-first, sauber im Scope. 3 MED / 2 LOW
eingearbeitet; die Prüf-Aufträge bestätigt.

## HIGH
Keine.

## MED (eingearbeitet)

- **MED-1 — Per-Datei-Zählung.** Real (gemessen) lastenheft **10** · spezifikation **19** ·
  architecture 1 (Summe 30 stimmt) — der Plan schrieb 11/18. Korrektur: Zahlen auf 10/19;
  DoD an »jede vom docs-check gemeldete Zeile markieren« gebunden, nicht an die Tabellenzahl.
- **MED-2 — Marker vs. Umformulierung.** Ein Teil der 19 spezifikation-Fundstellen ist **kein**
  reiner Verifikations-Zeiger, sondern substanzieller Fachverweis (z. B. »der slice-012-Teilumfang
  behandelt…«, »Port-Mechanik (slice-015b, entschieden)«, »ist slice-019b«). Ein
  `status-provenance`-Marker **unterdrückt** dort den SDP-Befund, **löst** die stabile→volatile-
  Kopplung aber nicht. Korrektur: Pro-Zeile-Triage — reine Reifephase-Provenance (»geschärft in
  slice-013a«) → Marker; substanzielle Verweise → selbsttragend umformulieren (Slice-Zeiger raus,
  Fachinhalt bleibt), analog zur architecture.md-Entfernung. Umgesetzt: 12 Marker + 16
  Umformulierungen + 1 Entfernung.
- **MED-3 — Marker-Wirkung auf Spec-Straten empirisch unbelegt.** Der a-check-Kommentar formuliert
  den Marker »im ADR-Körper«; b-cads Config sagt »ADR-/Spec-Körper«. Korrektur: vor der 30er-
  Remediation an EINER Spec-Zeile getestet → Marker klärte den Befund (Spec-Straten-wirksam
  bestätigt). Akzeptanz an den **post-remediation** docs-check-0-Lauf gebunden; Fallback (Auslagern/
  Umformulieren) benannt.

## LOW (eingearbeitet)

- **LOW-1 — MR-014-Lineage.** MR-014 nennt MR-011 ausdrücklich als erweiterte Adaption
  (Lineage-Zeile ergänzt).
- **LOW-2 — Versions-Verfügbarkeit.** »token/exempt-paths/status-provenance ab v0.31« als Annahme
  gekennzeichnet; funktional durch den docs-check-0-Lauf unter v0.37.1 belegt.

## Bestätigte Prüf-Aufträge (INFO)

- **Grandfathering-Glob korrekt, kein Off-by-one:** `000[1-9]-*.md` + `001[0-7]-*.md` deckt exakt
  0001–0017; ADR-0018/0019/0020 nicht erfasst (korrekt außen vor). b-cad hat genau 17 Accepted-ADRs.
- **architecture.md-Sonderfall geboten:** Zeile 87 (`…WAL-006.a/slice-012`) verletzt bereits
  AGENTS §2.7 (»keine Slices«); die Token-Verschärfung deckt eine latente Alt-Inkonsistenz auf.
  Entfernung (statt Marker) ist §2.7-konform; der WAL-006-Link bleibt. Marker im Lastenheft ändern
  den abnahmebindenden Wortlaut nicht (unsichtbarer HTML-Kommentar) — kein MR-010-Nachzug nötig.
- **Verschärfung ≠ Lockerung (kein ADR):** Präzedenz MR-011/MR-013 (§2.6 betrifft nur
  Schwellen-Senkung; eine neue Matrix-Kante ist eine Restriktion).
- **Quellen-Konsistenz:** MR-014 ist die nächste freie Nummer (conventions.md endet bei MR-013);
  die zitierten Slice-Pläne (018a/027/030) existieren in done-archive; LH-FA-WAL-006-Anker existiert;
  Pin-Digest im Plan == tools/Dockerfile (sha256:3bbdb19b…); a-check-Referenzconfig trägt tatsächlich
  token + adr→slice + Grandfathering.
- **DoD-Beobachtbarkeit:** »docs-check 0 Befunde nach Remediation« ist ein realer Sensor
  (`make docs-check`/`make gates`).
- **Mehr-ID-Zeilen benign:** spezifikation:355/956, lastenheft:1027 tragen neben dem Slice-Token
  einen korrekt verlinkten Co-Identifier; der matrix-scoped Marker unterdrückt nur den Slice-Befund,
  die ids-Linkpflicht bleibt.
- **Scope sauber:** nur Tooling/Doku (Dockerfile, .d-check.yml, drei Spec-Straten [nur Marker/1
  Entfernung, kein AK-/Werte-Eingriff], conventions/README/AGENTS/CHANGELOG, docs/reviews). Kein src/,
  kein Schema, keine LH-Anforderung. DRW (032a/b/c) korrekt pausiert. Eigenes harness-steering-
  Quergewerk regelkonform (Muster MR-007/MR-011/MR-013, slice-018a/030).
